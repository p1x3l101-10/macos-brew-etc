export VISUAL=vim
export EDITOR=$VISUAL
export HOMEBREW_CASK_OPTS="--appdir=$(brew --prefix)/Appliations --no-quarantine"
if [[ -f "$(brew --prefix)/etc/proxy" ]]; then
  export all_proxy="$(cat "$(brew --prefix)/etc/proxy")"
fi
